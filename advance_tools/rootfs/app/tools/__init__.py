# Advance Tools — tool plugins live here.
# Each tool is a directory containing:
#   manifest.json — id, name, icon, description, color, page, version
#   tool.py       — register(app, ctx, manifest): add aiohttp routes
#   static/       — optional; served at /tools/<id>/static/
